<div class= "ice-box">
	<h2>
		OMG add your space cat, son!
	</h2>

	<%= simple_form_for @pic do |f| %>
		<%= f.input :title %>
		<%= f.input :message %>
		<%= f.input :who %>
		<%= f.button :submit %>
	<% end %>
</div>